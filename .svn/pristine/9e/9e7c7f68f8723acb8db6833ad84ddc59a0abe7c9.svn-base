<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.spring.dao.idpwDAO">
    
    	<select id="execSelect" resultType="int">
    		select count(email)
    		from member
    		where email = #{email}
    		and	  member_name = #{member_name}
    	</select>
    	<select id="execId" resultType="String">
    		select member_id
    		from member
    		where email = #{email}
    		and	  member_name = #{member_name}
    	</select>
    	<select id="execSelect2" resultType="int">
    		select count(email)
    		from member
    		where email = #{email}
    		and	  member_name = #{member_name}
    		and	  member_id = #{member_id}
    	</select>
    	<select id="execPw" resultType="String">
    		select member_pw
    		from member
    		where email = #{email}
    		and	  member_name = #{member_name}
    		and	  member_id = #{member_id}
    	</select>
    </mapper>